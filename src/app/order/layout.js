export const metadata = {
  title: "Request Solution Quote & Scope Consultation | ShubDeep Labs",
  description: "Request a custom project quote or solution consultation from ShubDeep Labs engineering desk.",
  alternates: {
    canonical: "https://shubh-deep-labs.vercel.app/order",
  },
};

export default function OrderLayout({ children }) {
  return children;
}
